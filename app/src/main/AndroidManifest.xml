<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.summerbot.lineagestoragefix">

    <application
        android:allowBackup="false"
        android:extractNativeLibs="false"
        android:hasCode="true"
        android:label="Lineage Storage Fix"
        android:description="Fix Storage page total/used values by reading real filesystem size.">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <meta-data
            android:name="xposeddescription"
            android:value="Fix Storage page total/used values by reading real filesystem size." />

        <meta-data
            android:name="xposedminversion"
            android:value="82" />
    </application>

</manifest>
